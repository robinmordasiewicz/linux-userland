---
- name: "Linux userland config"
  hosts: all
  become: true
  become_user: ubuntu
  gather_facts: true

  tasks:
    - name: Hushlogin
      file:
        path: "~/.hushlogin"
        state: touch
        mode: '0644'
