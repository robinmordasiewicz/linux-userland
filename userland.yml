---
- name: "Linux userland config"
  hosts: localhost
  become: true
  become_user: ubuntu
  connection: local
  gather_facts: true

- hosts: all
  tasks:
  - name: 
    file: .hushlogin
      path: "~/.hushlogin"
      state: touch
